// any element that apperes > n/2 ;

1) hashing 

2) Moone's algo  // 
     
      int cnt = 0 ;
      int el ;
      int n = nums.size();
      if(n<2)return nums[0];

      for(int i = 0 ;i< n ; i++ )
      {
        if(cnt == 0)
        {
            el = nums[i];
            // cnt = 1 ;
        }
        if(nums[i]==el) cnt++;
        else cnt-- ;

      }
   

    return el;
